apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-knowledge-base
  labels:
    app.kubernetes.io/name: knowledge-base
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: knowledge-base
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: knowledge-base
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      serviceAccountName: {{ .Release.Name }}-knowledge-base
      {{- if .Values.priorityClassName }}
      priorityClassName: {{ .Values.priorityClassName }}
      {{- end }}
      {{- if .Values.topologySpreadConstraints.enabled }}
      topologySpreadConstraints:
        - maxSkew: {{ .Values.topologySpreadConstraints.maxSkew }}
          topologyKey: {{ .Values.topologySpreadConstraints.topologyKey }}
          whenUnsatisfiable: {{ .Values.topologySpreadConstraints.whenUnsatisfiable }}
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: knowledge-base
              app.kubernetes.io/instance: {{ .Release.Name }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: knowledge-base
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            {{- toYaml .Values.containerSecurityContext | nindent 12 }}
          ports:
            - name: http
              containerPort: 8088
          env:
            - name: KNOWLEDGE_BASE_TENANT_ID
              value: {{ .Values.config.tenantId | quote }}
            - name: KNOWLEDGE_BASE_MULTI_TENANCY_BASE_URL
              value: {{ .Values.multiTenancyBaseUrl | quote }}
            - name: KNOWLEDGE_BASE_TELEMETRY__LOG_LEVEL
              value: {{ .Values.config.logLevel | quote }}
            - name: KNOWLEDGE_BASE_TELEMETRY__OTLP_ENDPOINT
              value: {{ .Values.config.otlpEndpoint | quote }}
            - name: KNOWLEDGE_BASE_CHUNKING__DEFAULT_STRATEGY
              value: {{ .Values.config.defaultChunkingStrategy | quote }}
            - name: KNOWLEDGE_BASE_STALENESS__DEFAULT_THRESHOLD_DAYS
              value: {{ .Values.config.staleThresholdDays | quote }}
            - name: KNOWLEDGE_BASE_VECTOR_DB_BASE_URL
              value: {{ .Values.vectorDbBaseUrl | quote }}
            - name: KNOWLEDGE_BASE_GRAPH_DB_BASE_URL
              value: {{ .Values.graphDbBaseUrl | quote }}
            {{- if .Values.database.existingSecret }}
            - name: KNOWLEDGE_BASE_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.database.existingSecret }}
                  key: {{ .Values.database.existingSecretKey }}
            {{- else }}
            - name: KNOWLEDGE_BASE_DATABASE_URL
              value: {{ .Values.database.url | quote }}
            {{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          startupProbe:
            {{- toYaml .Values.startupProbe | nindent 12 }}
          livenessProbe:
            {{- toYaml .Values.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.readinessProbe | nindent 12 }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
