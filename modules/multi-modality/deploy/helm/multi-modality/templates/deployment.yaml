apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-multi-modality
  labels:
    app.kubernetes.io/name: multi-modality
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: multi-modality
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: multi-modality
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      containers:
        - name: multi-modality
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8107
          env:
            - name: MULTI_MODALITY_TENANT_ID
              value: {{ .Values.config.tenantId | quote }}
            - name: MULTI_MODALITY_TELEMETRY__LOG_LEVEL
              value: {{ .Values.config.logLevel | quote }}
            - name: MULTI_MODALITY_TELEMETRY__OTLP_ENDPOINT
              value: {{ .Values.config.otlpEndpoint | quote }}
            - name: MULTI_MODALITY_GUARDRAILS_BASE_URL
              value: {{ .Values.guardrailsBaseUrl | quote }}
            {{- if .Values.database.existingSecret }}
            - name: MULTI_MODALITY_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.database.existingSecret }}
                  key: {{ .Values.database.existingSecretKey }}
            {{- else }}
            - name: MULTI_MODALITY_DATABASE_URL
              value: {{ .Values.database.url | quote }}
            {{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          livenessProbe:
            {{- toYaml .Values.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.readinessProbe | nindent 12 }}
