image:
  repository: tectonic/guardrails
  tag: "0.1.0"
  pullPolicy: IfNotPresent

replicaCount: 4

resources:
  requests:
    cpu: "1"
    memory: 1Gi
  limits:
    cpu: "1"
    memory: 1Gi

autoscaling:
  enabled: true
  minReplicas: 4
  maxReplicas: 30
  targetCPUUtilizationPercentage: 70

service:
  type: ClusterIP
  port: 8093

config:
  tenantId: "default"
  logLevel: "INFO"
  otlpEndpoint: "http://tempo:4317"
  groundednessThreshold: 0.85
  redTeamEnabled: true

database:
  url: "postgresql+asyncpg://guardrails:guardrails@postgres:5432/guardrails"
  existingSecret: ""
  existingSecretKey: "database-url"

llmGatewayBaseUrl: "http://llm-gateway:8082"
sentinelAgentsBaseUrl: "http://sentinel-agents:8094"
multiTenancyBaseUrl: "http://multi-tenancy:8109"

podDisruptionBudget:
  minAvailable: 3

livenessProbe:
  httpGet:
    path: /healthz
    port: 8093
  initialDelaySeconds: 5
  periodSeconds: 10

readinessProbe:
  httpGet:
    path: /healthz
    port: 8093
  initialDelaySeconds: 5
  periodSeconds: 10
