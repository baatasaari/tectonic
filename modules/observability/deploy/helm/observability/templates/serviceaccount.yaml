apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Release.Name }}-observability
  labels:
    app.kubernetes.io/name: observability
    app.kubernetes.io/instance: {{ .Release.Name }}
automountServiceAccountToken: {{ .Values.serviceAccount.automountServiceAccountToken }}
