apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-data-source-plugins
  labels:
    app.kubernetes.io/name: data-source-plugins
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: data-source-plugins
    app.kubernetes.io/instance: {{ .Release.Name }}
