apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Release.Name }}-auditability
  labels:
    app.kubernetes.io/name: auditability
    app.kubernetes.io/instance: {{ .Release.Name }}
automountServiceAccountToken: {{ .Values.serviceAccount.automountServiceAccountToken }}
